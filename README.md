# Billing Platform Monorepo (NestJS, gRPC, Consul)

This project implements a distributed billing platform using a monorepo architecture. It includes two gRPC microservices (`user-service` and `invoice-service`), an HTTP gateway, and dynamic service discovery using HashiCorp Consul.